export const SET_CURRENT_BLOG_DETAIL = (state, payload) => {
  state.CURRENT_BLOG_DETAIL = payload
}

export const SET_BLOG_LIST = (state, payload) => {
  state.BLOG_LIST = payload
}

export const SET_CURRENT_NAV_NAME = (state, payload) => {
  state.CURRENT_NAV_NAME = payload
}
